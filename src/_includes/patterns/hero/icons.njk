<div class="hero-icons">
  {% for icon in hero.icons %}
    <span class="hero-icon" aria-label="{{ icon.label }}">
      <i class="fa-solid {{ icon.name }}" aria-hidden="true"></i>
      <span class="sr-only">{{ icon.label }}</span>
    </span>
  {% endfor %}
</div>
